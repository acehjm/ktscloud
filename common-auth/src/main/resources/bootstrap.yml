spring:
  main:
    allow-bean-definition-overriding: true
  application:
    name: common-auth
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848
      config:
        server-addr: 127.0.0.1:8848
        file-extension: yaml
        prefix: common-auth
server:
  port: 8902